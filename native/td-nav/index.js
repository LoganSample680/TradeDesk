// The app talks to this plugin through Capacitor.registerPlugin('TdNav')
// directly (js/drive.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
