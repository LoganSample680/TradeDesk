// The app talks to this plugin through Capacitor.registerPlugin('TdBgUp')
// directly (js/bg-upload.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
