// The app talks to this plugin through Capacitor.registerPlugin('TdPush')
// directly (js/push.js); this entry exists so the package resolves as a normal
// npm module during cap sync.
module.exports = {};
