// The app talks to this plugin through Capacitor.registerPlugin('TdLock')
// directly (js/handoff.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
