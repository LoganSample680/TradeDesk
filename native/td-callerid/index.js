// The app talks to this plugin through Capacitor.registerPlugin('TdCallerId')
// directly (js/callerid.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
