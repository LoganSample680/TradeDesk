// The app talks to this plugin through Capacitor.registerPlugin('TdGeo')
// directly (js/geo-track.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
