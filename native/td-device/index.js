// The app talks to this plugin through Capacitor.registerPlugin('TdDevice')
// directly (js/cloud.js _tdDeviceNative); this entry exists so the package
// resolves as a normal npm module during cap sync.
module.exports = {};
