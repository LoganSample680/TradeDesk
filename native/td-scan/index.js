// The app talks to this plugin through Capacitor.registerPlugin('TdScan')
// directly (js/scan.js); this entry exists so the package resolves as a
// normal npm module during cap sync.
module.exports = {};
