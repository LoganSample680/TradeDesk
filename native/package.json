{
  "name": "tradedesk-native-shell",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor shell for TradeDesk Beta. The web app itself is NOT bundled here: the shell loads the live UAT deployment (see capacitor.config.json server.url), so day-to-day web pushes ship instantly without an App Store release. This package only exists so CI can generate the iOS project and build it.",
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor-community/background-geolocation": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
