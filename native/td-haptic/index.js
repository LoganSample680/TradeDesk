// The app talks to this plugin through Capacitor.registerPlugin('TdHaptic')
// directly (js/utils.js _tdHaptic); this entry exists so the package resolves
// as a normal npm module during cap sync.
module.exports = {};
